services:
  emby:
    image: emby/embyserver:latest
    container_name: emby
    network_mode: host
    volumes:
      - /var/lib/emby:/config
      - /mnt/media:/mnt/media 
    devices:
      - /dev/dri:/dev/dri
    environment:
      - TZ=America/Denver
    restart: unless-stopped
