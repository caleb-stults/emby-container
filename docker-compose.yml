services:
  emby:
    image: emby/embyserver:latest
    container_name: emby
    network_mode: host
    volumes:
      - ${LOCAL_CONFIG}:/config
      - ${MEDIA_LIBRARY}:/mnt/media
    devices:
      - /dev/dri:/dev/dri
    environment:
      - TZ=America/Denver
    restart: unless-stopped
